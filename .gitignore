# Local documentation — must NEVER be published to GitHub.
# Re:HomeProxy AutoMod change log / working notes live in LOCAL_DOCS/.
LOCAL_DOCS/
LOCAL_CHANGELOG.md
LOCAL_*.md

# Go helper build artifacts (built in CI, never committed)
.github/go/*/bin/
